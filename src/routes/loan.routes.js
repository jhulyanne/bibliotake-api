// importado apenas o objeto router do express, e não ele inteiro
import { Router } from "express";

// rotas prováveis:
/*
    - get pra retornar todos os empréstimos
    - get pra retornar todos os empréstimos feitos por um usuário (nome)
*/

const loans = Router();
